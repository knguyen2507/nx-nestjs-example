{
  "name": "upload-server",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/upload-server/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "upload-server:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "upload-server:build:development"
        },
        "production": {
          "buildTarget": "upload-server:build:production"
        }
      }
    }
  }
}
